export const DEFAULT_DATA_ROOT = 'F:/AINovelistData'

export const DEFAULT_PATHS = {
  workspace: `${DEFAULT_DATA_ROOT}/workspace`,
  knowledgeBase: `${DEFAULT_DATA_ROOT}/knowledge_base`,
  models: `${DEFAULT_DATA_ROOT}/models`,
  cache: `${DEFAULT_DATA_ROOT}/cache`
} as const
